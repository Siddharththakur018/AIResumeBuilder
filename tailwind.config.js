module.exports = {
    theme: {
      extend: {
        colors: {
          primary: '#ff5733', // Custom color
        },
        fontFamily: {
          custom: ["'Anton'", 'sans-serif'], // Ensure correct formatting
        }
      }
    },
    plugins: [],
};
